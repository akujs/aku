{
  "type": "module",
  "private": true,
  "workspaces": [
    "test-apps/*"
  ],
  "engines": {
    "node": ">=24.10.1",
    "bun": ">=1.3.5"
  },
  "scripts": {
    "check": "bun run --cwd packages/aku check && bun run --cwd packages/aku build && bun run --cwd test-apps/nextjs check"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.5",
    "@types/bun": "1.2.21",
    "bun-types": "1.2.21"
  }
}
