{
  "extends": "./tsconfig.common.json",
  "compilerOptions": {
    // FIXME - should be ES2025, update once ts releases official v6/7 and we update tooling to match
    "lib": ["ESNext"],
    "types": ["node"],
    "erasableSyntaxOnly": true
  },
  "include": ["src/**/*.ts", "src/**/*.node-test.ts"],
  "exclude": ["src/**/*.bun.ts", "src/**/*.test.ts", "src/**/*.test-utils.ts"]
}
